I would like to have a setting that allows me to configure which partitions of a database to keep. For example, I could specify [1-999] to say that I want to save data related to {{db}}_i for i=1..999. I also would like to change the api to return an error message if a hash query doesn't fall within the partitions we support. There should also be validation that we don't exceed the max number of partitions. This should all be optional too so if not provided, all partitions are maintained